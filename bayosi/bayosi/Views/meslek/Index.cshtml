@model IEnumerable<bayosi.Models.meslek>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.meslek_adi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.meslek_açiklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.meslek_adi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.meslek_açiklama)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.meslek_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.meslek_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.meslek_id })
        </td>
    </tr>
}

</table>
