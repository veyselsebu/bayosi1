@model IEnumerable<bayosi.Models.yonetici>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            ad
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.yoneticiAd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.yoneticiAd }) |
            @Html.ActionLink("Details", "Details", new { id=item.yoneticiAd }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.yoneticiAd })
        </td>
    </tr>
}

</table>
