@model IEnumerable<bayosi.Models.ilac>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ilac_ad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.aciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ilac_ad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.aciklama)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ilac_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.ilac_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ilac_id })
        </td>
    </tr>
}

</table>
